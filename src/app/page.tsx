import styles from "@/styles/pageHome.module.css";

export default function HomePage() {
    return (
        <main className={styles.main}>
            <h1 className={styles.header}>Parker Botsford</h1>
        </main>
    );
}
