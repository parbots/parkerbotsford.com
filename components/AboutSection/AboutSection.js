import styles from './AboutSection.module.css';

const AboutSection = () => {
    return (
        <section className={styles.aboutSection}>
            <h2>About and Skills Section</h2>
        </section>
    );
};

export default AboutSection;
